# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Secrets / environment
.env
.env.*
!.env.example

# Logs
*.log
logs/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Node
node_modules/
dist/
build/
.next/
coverage/

# stlite build output (regenerated by scripts/build-pwa.mjs on every Vercel build)
pwa/

# Data / artifacts
.data/
gateway/data/
*.csv
*.xlsx
*.parquet
*.pkl
*.joblib
artifacts/
outputs/
gateway/config/config.yaml
firmware/secrets.h

# tailscale cert 가 현재 디렉터리에 떨구는 인증서·개인키.
# 저장소에 절대 들어가면 안 된다 (Funnel 은 자체적으로 인증서를 관리하므로
# 이 파일들을 직접 쓸 일도 없다).
*.ts.net.crt
*.ts.net.key
