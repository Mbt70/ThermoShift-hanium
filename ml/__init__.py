"""ThermoShift 모델링 계층.

게이트웨이(gateway/)는 실시간 판단만 한다. 파라미터를 **추정**하는 일은
여기서 오프라인으로 하고, 결과를 ml/params/*.json 으로 떨궈 게이트웨이가
읽는다. 둘을 나눈 이유는 두 가지다.

  - 게이트웨이는 라즈베리파이에서 30초마다 돌아야 한다. 학습을 그 안에
    넣으면 판단 주기를 지킬 수 없다.
  - 파라미터가 파일로 남아야 "이 판단이 어떤 모델에서 나왔는지" 를
    나중에 대조할 수 있다. 발표에서 근거로 쓸 수 있어야 한다.
"""
