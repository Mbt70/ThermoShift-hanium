#pragma once

// Copy this file to firmware/secrets.h and replace the values locally.
// firmware/secrets.h is ignored by git.
#define THERMOSHIFT_WIFI_SSID "replace-me"
#define THERMOSHIFT_WIFI_PASSWORD "replace-me"
